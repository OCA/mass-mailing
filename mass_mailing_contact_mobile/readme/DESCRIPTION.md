This module improves the ``mass_maing`` module, adding ``mobile`` field
on ``mailing.contact`` model.

This field is defined in Odoo ``mass_mailing_sms`` but this simple module
allow to have this field, without install ``mass_mailing_sms`` and all the
related dependencies ``sms``, ``iap``, etc...
